'use strict';

var _ochreLiberator = require('ochre-liberator');

function getDocInfo(file, cb) {
    (0, _ochreLiberator.getFileContents)(file, function (err, contents) {
        cb(err, { contents: contents });
    });
}

module.exports = {
    resolve: getDocInfo,
    types: ['.doc', '.docx', '.txt', '.rtf', '.ppt', '.pptx', '.xls', '.xlsx', '.pdf']
};